<h1>
  Posts
  <% if @subject %>
    of <%= link_to @subject.name, subject_path(@subject) %>
  <% end %>
</h1>

<% if @subject %>
  <%= link_to 'new post', new_subject_post_path(@subject) %>
<% end %>

<ul>
  <%= render @posts %>
</ul>
